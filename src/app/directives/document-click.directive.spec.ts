import { DocumentClickDirective } from './document-click.directive';

describe('DocumentClickDirective', () => {
  it('should create an instance', () => {
    const directive = new DocumentClickDirective();
    expect(directive).toBeTruthy();
  });
});
